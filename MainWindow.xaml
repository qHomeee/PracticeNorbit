<Window x:Class="AutoPrint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AutoPrint" Height="350" Width="600">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Папка -->
        <StackPanel Orientation="Horizontal">
            <TextBox Width="350" Name="FolderBox" IsReadOnly="True"/>
            <Button Width="120" Margin="10,0,0,0" Click="SelectFolder_Click">
                Выбрать папку
            </Button>
        </StackPanel>

        <!-- Принтер -->
        <ComboBox Grid.Row="1" Name="PrinterBox" Height="25" Margin="0,10,0,10"/>

        <!-- Лог -->
        <ListBox Grid.Row="2" Name="LogBox"/>

    </Grid>
</Window>